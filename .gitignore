# dependencies
node_modules
.pnp
.pnp.*

# next.js
/.next/
/out/
next-env.d.ts
*.tsbuildinfo

# production
/build
dist
*.tgz

# logs
logs
*.log
npm-debug.log*

# dotenv environment variable files — never commit secrets
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# caches
.eslintcache
.cache

# audit output — screenshots and raw measurement JSON. The repo is public and
# no binary belongs in it; `docs/MOBILE-AUDIT.md` is the committed deliverable.
.audit/

# vercel
.vercel

# editors / OS
.idea
.vscode
.DS_Store


# local Claude Code tooling config (per-machine, not project source)
.claude/settings.json
.claude/settings.local.json
